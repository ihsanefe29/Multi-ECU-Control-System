import os  import matplotlib.pyplot as plt import numpy as np import pandas as pd import seaborn as sns from datetime import datetime   # ========================================================== # SAVE FIGURE # ==========================================================  def _save_figure(filename: str):      os.makedirs(         "figures",         exist_ok=True     )      timestamp = datetime.now().strftime(         "%Y%m%d_%H%M%S"     )      name, extension = os.path.splitext(         filename     )      new_filename = (         f"{name}_{timestamp}{extension}"     )      path = os.path.join(         "figures",         new_filename     )      plt.savefig(         path,         dpi=300,         bbox_inches="tight"     )      print(         f"Figure saved: {path}"     )  # ========================================================== # COLUMN HELPERS # ==========================================================  def get_numeric_columns(df: pd.DataFrame) -> list:     return df.select_dtypes(include=np.number).columns.tolist()   def get_categorical_columns(df: pd.DataFrame) -> list:     return df.select_dtypes(exclude=np.number).columns.tolist()    def select_numeric_columns(     numeric_columns,     minimum=1 ):     print("\nAvailable Numeric Columns:\n")      for i, column in enumerate(         numeric_columns,         start=1     ):         print(f"{i}. {column}")      print("all. All Numeric Columns")     print("0. Cancel")      while True:          selected = input(             "\nEnter column numbers separated by commas "             "(example: 1,3,5), 'all', or 0: "         ).strip().lower()          if selected == "0":             return None          if selected == "all":             return numeric_columns          try:              indexes = [                 int(i.strip()) - 1                 for i in selected.split(",")             ]              columns = [                 numeric_columns[i]                 for i in indexes             ]              if len(columns) < minimum:                 print(                     f"Please select at least "                     f"{minimum} columns."                 )                 continue              return columns          except (ValueError, IndexError):              print("Invalid selection.")   # ========================================================== # HISTOGRAM # ==========================================================  def plot_histogram(     df: pd.DataFrame,     columns: list,     bins: int = 30 ):     plt.figure(figsize=(8, 5))      for column in columns:         plt.hist(             df[column].dropna(),             bins=bins,             alpha=0.4,             label=column         )      if len(columns) > 1:         plt.legend()      plt.title("Histogram Comparison")     plt.xlabel("Value")     plt.ylabel("Frequency")      plt.tight_layout()      _save_figure("histogram.png")      plt.show()     plt.close()   # ========================================================== # BOXPLOT # ==========================================================  def plot_boxplot(     df: pd.DataFrame,     columns: list ):     plt.figure(figsize=(10, 5))      sns.boxplot(         data=df[columns]     )      plt.xticks(rotation=45)      plt.title("Boxplot Comparison")      plt.tight_layout()      _save_figure("boxplot.png")      plt.show()     plt.close()  # ========================================================== # DISTRIBUTION PLOT # ==========================================================  def plot_distribution(     df: pd.DataFrame,     columns: list ):     plt.figure(figsize=(8, 5))      for column in columns:          sns.kdeplot(             df[column].dropna(),             label=column         )      if len(columns) > 1:         plt.legend()      plt.title("Distribution Comparison")      plt.tight_layout()      _save_figure("distribution.png")      plt.show()     plt.close() # ========================================================== # SCATTER PLOT # ==========================================================  def plot_scatter(     df: pd.DataFrame,     x_column: str,     y_column: str,     save: bool = False ):     plt.figure(figsize=(8, 5))      sns.scatterplot(         data=df,         x=x_column,         y=y_column     )      plt.title(f"{x_column} vs {y_column}")     plt.xlabel(x_column)     plt.ylabel(y_column)      plt.tight_layout()      if save:         _save_figure("scatterplot.png")      plt.show()     plt.close()  def plot_numeric_bar(     df: pd.DataFrame,     columns: list ):     values = df[columns].mean()      plt.figure(figsize=(8, 5))      values.plot(         kind="bar"     )      plt.title(         "Numeric Column Mean Comparison"     )      plt.ylabel(         "Mean"     )      plt.xticks(         rotation=45     )      plt.tight_layout()      _save_figure(         "numeric_barplot.png"     )      plt.show()     plt.close()   # ========================================================== # BAR PLOT # ==========================================================  def plot_bar(     df: pd.DataFrame,     category_column: str,     value_column: str ):     plt.figure(figsize=(10, 5))      sns.barplot(         data=df,         x=category_column,         y=value_column     )      plt.xticks(rotation=45)      plt.title(         f"{value_column} by {category_column}"     )      plt.tight_layout()      _save_figure("barplot.png")      plt.show()     plt.close()  # ========================================================== # CORRELATION HEATMAP # ==========================================================  def plot_heatmap(     df: pd.DataFrame,     columns: list | None = None,     annot: bool = True ):     numeric_df = df.select_dtypes(include=np.number)      if columns is not None:         numeric_df = numeric_df[columns]      if numeric_df.empty:         print("No numeric columns available.")         return      plt.figure(figsize=(10, 8))      sns.heatmap(         numeric_df.corr(),         annot=annot,         cmap="coolwarm",         fmt=".2f"     )      plt.title("Correlation Heatmap")      plt.tight_layout()      _save_figure("heatmap.png")      plt.show()     plt.close()  # ========================================================== # PAIRPLOT # ==========================================================  def plot_pairplot(     df: pd.DataFrame,     columns: list,     sample_size: int | None = None ):     pairplot_df = df[columns].dropna()      if sample_size is not None:          sample_size = min(             sample_size,             len(pairplot_df)         )          pairplot_df = pairplot_df.sample(             n=sample_size,             random_state=42         )      pairplot = sns.pairplot(         pairplot_df     )      os.makedirs(         "figures",         exist_ok=True     )      pairplot.savefig(         "figures/pairplot.png",         dpi=300     )      plt.show()     plt.close()  # ========================================================== # VISUALIZATION MENU # ==========================================================  def visualize(df: pd.DataFrame):     """     Interactive visualization menu.     """      numeric_columns = get_numeric_columns(df)     categorical_columns = get_categorical_columns(df)      if not numeric_columns:         print("No numeric columns found.")         return      while True:          print("\n" + "=" * 40)         print("        Visualization Menu")         print("=" * 40)          print("1. Histogram")         print("2. Boxplot")         print("3. Distribution Plot")         print("4. Scatter Plot")         print("5. Bar Plot")         print("6. Correlation Heatmap")         print("7. Pairplot")         print("8. All Visualizations")         print("0. Exit")          choice = input(             "\nSelect an option: "         ).strip()          # ==================================================         # HISTOGRAM / BOXPLOT / DISTRIBUTION         # ==================================================          if choice in ["1", "2", "3"]:              columns = select_numeric_columns(                 numeric_columns,                 minimum=1             )              if columns is None:                 continue              if choice == "1":                  plot_histogram(                     df,                     columns                 )              elif choice == "2":                  plot_boxplot(                     df,                     columns                 )              else:                  plot_distribution(                     df,                     columns                 )          # ==================================================         # SCATTER         # ==================================================          elif choice == "4":              columns = select_numeric_columns(                 numeric_columns,                 minimum=2             )              if columns is None:                 continue              if len(columns) != 2:                  print(                     "Scatter plot requires "                     "exactly two columns."                 )                  continue              plot_scatter(                 df,                 columns[0],                 columns[1]             )          # ==================================================         # BAR         # ==================================================          elif choice == "5":              if not categorical_columns:                  print(                     "No categorical columns found."                 )                  continue              print(                 "\nAvailable Categorical Columns:\n"             )              for i, column in enumerate(                 categorical_columns,                 start=1             ):                 print(                     f"{i}. {column}"                 )              try:                  cat_index = int(                     input(                         "\nSelect category column: "                     )                 ) - 1                  columns = select_numeric_columns(                     numeric_columns,                     minimum=1                 )                  if columns is None:                     continue                  # Bar plot bir kategorik ve bir say─▒sal                 # kolon kullan─▒yor.                 value_column = columns[0]                  plot_bar(                     df,                     categorical_columns[cat_index],                     value_column                 )              except (                 ValueError,                 IndexError             ):                  print(                     "Invalid selection."                 )          # ==================================================         # HEATMAP         # ==================================================          elif choice == "6":              columns = select_numeric_columns(                 numeric_columns,                 minimum=2             )              if columns is None:                 continue              annot = input(                 "\nShow correlation values? (y/n): "             ).strip().lower() == "y"              plot_heatmap(                 df,                 columns=columns,                 annot=annot             )          # ==================================================         # PAIRPLOT         # ==================================================          elif choice == "7":              columns = select_numeric_columns(                 numeric_columns,                 minimum=2             )              if columns is None:                 continue              sample = input(                 "\nSample size "                 "(Press Enter for all rows): "             ).strip()              try:                  sample_size = (                     int(sample)                     if sample                     else None                 )              except ValueError:                  print(                     "Invalid sample size."                 )                  continue              plot_pairplot(                 df,                 columns=columns,                 sample_size=sample_size             )          # ==================================================         # ALL VISUALIZATIONS         # ==================================================          elif choice == "8":              columns = select_numeric_columns(                 numeric_columns,                 minimum=1             )              if columns is None:                 continue              print(                 "\nGenerating all visualizations..."             )              plot_histogram(                 df,                 columns             )              plot_boxplot(                 df,                 columns             )              plot_distribution(                 df,                 columns             )              if len(columns) >= 2:                  plot_heatmap(                     df,                     columns=columns,                     annot=True                 )                  sample_size = min(                     1000,                     len(df)                 )                  plot_pairplot(                     df,                     columns=columns,                     sample_size=sample_size                 )              else:                  print(                     "\nHeatmap and Pairplot skipped "                     "because at least two columns "                     "are required."                 )              print(                 "\nAll visualizations completed."             )          # ==================================================         # EXIT         # ==================================================          elif choice == "0":              print(                 "\nVisualization menu closed."             )              break          else:              print(                 "Invalid selection."             )